"""Test-only conftest. The plugin itself registers fixtures via entry points,
so tests should work whether or not this conftest exists — but we keep it as
a stable place to add shared test-helpers later."""
